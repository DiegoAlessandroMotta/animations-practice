---
import IconContainer from "./IconContainer.astro";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<button
  class=`bg-transparent flex items-stretch cursor-pointer text-white/50 transition duration-300 [&:hover>figure]:before:opacity-10 ${className}`
>
  <slot name="before" />
  <IconContainer>
    <slot name="icon" />
  </IconContainer>
  <slot />
</button>
